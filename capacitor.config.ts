import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'WispSmart',
  webDir: 'dist/front-netplay/browser'
};

export default config;
